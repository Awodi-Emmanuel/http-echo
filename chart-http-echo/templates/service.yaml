apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-http-echo.fullname" . }}
  labels:
    {{- include "chart-http-echo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-http-echo.selectorLabels" . | nindent 4 }}
