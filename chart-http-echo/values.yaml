# Default values for chart-http-echo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

image:
  name: img-http-echo
  repository: awodi2525/img-http-echo:latest
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v1"



service:
  type: LoadBalancer
  port: 5000
  targetPort: 5678

